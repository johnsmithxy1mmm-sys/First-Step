# Финансовый движок
numpy==2.3.4

# Валидация входного YAML
pydantic==2.12.4
PyYAML==6.0.3

# Шаблон отчёта и рендер
jinja2==3.1.6
matplotlib==3.10.8

# Рендер HTML -> PDF через headless Chromium.
# ВАЖНО: версия привязана к ревизии браузера 1194 (Chromium 141).
# После установки выполнить: playwright install chromium
playwright==1.56.0

# Тесты финансового модуля
pytest==9.0.1
