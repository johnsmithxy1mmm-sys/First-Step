# Финансовый движок
numpy>=1.26

# Валидация входного YAML
pydantic>=2.9
PyYAML>=6.0

# Шаблон отчёта и рендер
jinja2>=3.1
matplotlib>=3.8

# Рендер HTML -> PDF через headless Chromium.
# Версию фиксируем: она проверена и согласована с движком рендера.
# После установки выполнить: playwright install chromium
playwright==1.56.0

# Тесты финансового модуля
pytest>=8.0
