httpx>=0.27
pydantic>=2.5
PyYAML>=6.0
APScheduler>=3.10
rich>=13.0
python-dotenv>=1.0
websockets>=12.0         # WS-стаканы (мгновенные котировки/выходы)
py-clob-client>=0.17     # подписанные ордера (только live); проверьте V2 в Фазе 0
anthropic>=0.40          # LLM-сигнал (только при estimator.llm.enabled)
pytest>=8.0              # тесты
