httpx>=0.27
pydantic>=2.5
PyYAML>=6.0
APScheduler>=3.10
rich>=13.0
python-dotenv>=1.0
websockets>=12.0         # WS order books (instant quotes/exits)
py-clob-client>=0.17     # signed orders (live only); verify V2 in Phase 0
anthropic>=0.40          # LLM signal (only when estimator.llm.enabled)
pytest>=8.0              # tests
