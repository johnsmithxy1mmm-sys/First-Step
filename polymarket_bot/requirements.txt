httpx>=0.27
pydantic>=2.5
PyYAML>=6.0
APScheduler>=3.10
rich>=13.0
python-dotenv>=1.0
py-clob-client>=0.17     # подписанные ордера (только --mode live)
anthropic>=0.40          # LLM-сигнал (только при estimator.llm.enabled)
pytest>=8.0              # тесты
